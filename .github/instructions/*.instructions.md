# GitHub Copilot Custom Instructions: WECFM Orchestra

## 1. Architectural Context
- You are writing code for the **WECFM Orchestra**, operating under the broader **Project Charcuterie** AGI architecture and **WECRYPTO** prediction engine. 
- The system heavily relies on an **Orbital Coherence architecture**. When generating logic for data ingestion, prediction, or routing, map signals strictly to **s, p, d, and f orbitals**. 
- Maintain strict separation of concerns between these orbital layers. 

## 2. Local-First Execution
- All generated code must be compatible with a local **AMD Ryzen Pro TUF Win10 ProN** server environment first. 
- Assume local execution is the primary staging ground before any cloud deployment. Prioritize efficient local resource management and thread pooling tailored for AMD Ryzen architecture.

## 3. Tech Stack & Integration Rules
- **AI & Cloud:** Default to **Vertex AI** for model integration and **Firebase** for real-time state sync and user authentication.
- **Storage:** Use **TIDE** storage protocols for handling massive orbital datasets. 
- **Networking:** Assume internal routing is secured via **Tailscale**. Do not expose internal development ports to the public internet; use Tailscale IPs or localhost configurations.
- **Deployment:** The ultimate production target for front-end/edge components is **Vercel**. 

## 4. Security & Quality Assurance
- **Zero-Tolerance for Vulnerabilities:** All generated code must pass **Snyk** and **CodeQL** vulnerability scans natively. 
- Sanitize all inputs aggressively, especially when handling cryptocurrency parameters (e.g., Solana, Render, Polkadot, XRP, BTC, ETH). 
- Avoid deprecated libraries. If a Vertex AI or Firebase SDK update requires a structural change, write to the most modern standard.

## 5. UI/UX & Visualization Standards
- When generating frontend code or visualization components, strictly enforce **blue hex-based dynamic mapping**. 
- Standardize all color variables using **Pantone color codes** to maintain absolute visual consistency across the dashboard. 

## 6. Coding Style
- Write concise, modular, and strongly typed code. 
- Avoid verbose explanations in code comments; focus on the *why* rather than the *what*. 
- Prefix predictive algorithms and orbital logic modules with clear architectural tags (e.g., `s_orbital_ingest`, `f_orbital_coherence`).
